import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#F5F7FB",
      dark: "#e2e4e8",
    },
    secondary: {
      main: "#393D45",
    },
  },
});

export default theme;
